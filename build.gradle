apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "clock-time"
version = '1.0'
mainClassName = "ClockTime"

repositories {
 mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}